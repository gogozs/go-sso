version: '2'
services:
  web:
    container_name: go-sso
    build: .
    restart: always
    ports:
      - "8008:8008"
    environment:
      GO_SSO_WORKDIR: /app
      gin_env: production
      GIN_MODE: release